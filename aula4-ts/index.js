function calcularArea(lado) {
    return lado * lado;
}
let quadrado = calcularArea(2);
console.log(quadrado);

let aluno = {
    nome: 'João',
    mediaFinal: 7.5,
    situacao: 'aprovado'
};
console.log(aluno);
